name: 早间新闻

on:
  schedule:
    # 每天新加坡时间 08:00（UTC 00:00）运行
    - cron: '0 0 * * *'
  # 支持在 GitHub Actions 页面手动测试
  workflow_dispatch:

jobs:
  push-news:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: pip install requests

      - name: Push morning news
        env:
          FEISHU_WEBHOOK: ${{ secrets.FEISHU_WEBHOOK }}
        run: python daily_news.py

      # 任何前面的步骤失败，都自动通过飞书报警
      - name: Notify failure to Feishu
        if: ${{ failure() }}
        env:
          FEISHU_WEBHOOK: ${{ secrets.FEISHU_WEBHOOK }}
          GITHUB_RUN_URL: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          if [ -z "$FEISHU_WEBHOOK" ]; then
            echo "FEISHU_WEBHOOK 未设置，无法发送失败报警"
            exit 1
          fi

          MESSAGE="❌ 早间新闻 Workflow 执行失败\n\n仓库：${GITHUB_REPOSITORY}\n分支：${GITHUB_REF_NAME}\n运行编号：${GITHUB_RUN_NUMBER}\n时间：$(date -u '+%Y-%m-%d %H:%M:%S UTC')\n\n请检查 GitHub Actions：${GITHUB_RUN_URL}"

          python - "$FEISHU_WEBHOOK" "$MESSAGE" <<'PY'
          import json
          import sys
          import requests

          webhook = sys.argv[1]
          message = sys.argv[2]

          response = requests.post(
              webhook,
              json={"msg_type": "text", "content": {"text": message}},
              timeout=10,
          )
          response.raise_for_status()
          result = response.json()
          if result.get("code") != 0:
              raise RuntimeError(f"飞书返回错误: {result}")
          print("✅ 失败报警已发送到飞书")
          PY
